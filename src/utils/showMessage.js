/**
 * 弹出消息
 * @param {String} content 消息内容
 * @param {String} type 消息类型，info error success warn
 * @param {HTMLElement} container 容器，消息会显示到该容器的正中
 */
export default function (content, type, container) {}
